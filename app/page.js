import Hero from './Hero'
import Form from './Form';

export default function App() {
  
    return (
      <>
      <Hero />
      <Form />
      {/* {messages.map(m => (
        <div key={m.id}>
          {m.role === 'user' ? 'user: ': 'AI: '}
          {m.content}
          {}
        </div>
      ))} */}
    </>
    );
}
